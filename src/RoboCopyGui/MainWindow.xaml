<Window x:Class="RoboCopyGui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:core="clr-namespace:RoboCopyGui.Core"
        mc:Ignorable="d"
        Title="RoboCopy GUI"
        Width="1280" Height="830" MinWidth="1050" MinHeight="700"
        WindowStartupLocation="CenterScreen"
        Closing="Window_Closing">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="72" />
            <RowDefinition Height="*" />
            <RowDefinition Height="82" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
            <Grid Margin="24,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Border Width="42" Height="42" CornerRadius="10" Background="{DynamicResource AccentBrush}" VerticalAlignment="Center">
                    <TextBlock Text="⇄" Foreground="White" FontSize="24" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
                <StackPanel Grid.Column="1" Margin="13,0,0,0" VerticalAlignment="Center">
                    <TextBlock Text="RoboCopy GUI" FontSize="21" FontWeight="SemiBold" />
                    <TextBlock Text="Reliable Windows file transfers, without the command-line guesswork" Foreground="{DynamicResource MutedTextBrush}" Margin="0,2,0,0" />
                </StackPanel>
                <Border Grid.Column="2" Padding="12,6" CornerRadius="12" Background="{DynamicResource AccentSoftBrush}" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Stage}" Foreground="{DynamicResource AccentBrush}" FontWeight="SemiBold" />
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="20,18,20,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.95*" MinWidth="455" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="1.05*" MinWidth="500" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="14" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <TextBlock Text="Transfer locations" FontSize="16" FontWeight="SemiBold" />
                        <TextBlock Text="Local drives, mapped drives, and UNC network paths are supported." Foreground="{DynamicResource MutedTextBrush}" Margin="0,3,0,14" />

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="SOURCE" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource MutedTextBrush}" Margin="2,0,0,5" />
                                <TextBox Text="{Binding SourcePath, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="{Binding IsBusy}" AutomationProperties.Name="Source folder" />
                            </StackPanel>
                            <Button Grid.Column="1" Content="Browse…" Margin="8,22,0,0" Command="{Binding BrowseSourceCommand}" />
                        </Grid>

                        <Button Content="⇅  Swap source and destination" HorizontalAlignment="Center" Margin="0,9" Padding="14,6" Command="{Binding SwapCommand}" />

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="DESTINATION" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource MutedTextBrush}" Margin="2,0,0,5" />
                                <TextBox Text="{Binding DestinationPath, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="{Binding IsBusy}" AutomationProperties.Name="Destination folder" />
                            </StackPanel>
                            <Button Grid.Column="1" Content="Browse…" Margin="8,22,0,0" Command="{Binding BrowseDestinationCommand}" />
                        </Grid>

                        <TextBlock Text="FILE PATTERNS" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource MutedTextBrush}" Margin="2,13,0,5" />
                        <TextBox Text="{Binding FilePatterns, UpdateSourceTrigger=PropertyChanged}" ToolTip="Examples: *.*   *.jpg   &quot;annual report*.pdf&quot;" IsReadOnly="{Binding IsBusy}" />
                    </StackPanel>
                </Border>

                <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <StackPanel Margin="18,16,18,10">
                            <TextBlock Text="Copy options" FontSize="16" FontWeight="SemiBold" />
                            <TextBlock Text="Select an option to include its Robocopy switch. Conflicting choices are disabled." Foreground="{DynamicResource MutedTextBrush}" Margin="0,3,0,0" />
                        </StackPanel>

                        <ListBox Grid.Row="1" ItemsSource="{Binding OptionsView}" BorderThickness="0" Background="Transparent"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 VirtualizingPanel.IsVirtualizing="True"
                                 VirtualizingPanel.IsVirtualizingWhenGrouping="True">
                            <ListBox.GroupStyle>
                                <GroupStyle>
                                    <GroupStyle.HeaderTemplate>
                                        <DataTemplate>
                                            <Border Background="{DynamicResource SurfaceAlternativeBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,1" Padding="18,8" Margin="0,5,0,0">
                                                <TextBlock Text="{Binding Name}" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" />
                                            </Border>
                                        </DataTemplate>
                                    </GroupStyle.HeaderTemplate>
                                </GroupStyle>
                            </ListBox.GroupStyle>
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    <Setter Property="Padding" Value="0" />
                                    <Setter Property="Margin" Value="0" />
                                    <Setter Property="Background" Value="Transparent" />
                                    <Setter Property="BorderThickness" Value="0" />
                                    <Setter Property="Focusable" Value="False" />
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="ListBoxItem">
                                                <ContentPresenter />
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="18,10" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="24" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="105" />
                                            </Grid.ColumnDefinitions>
                                            <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsAvailable}" Margin="0,3,0,0" />
                                            <StackPanel Grid.Column="1" Margin="3,0,10,0">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding Label}" FontWeight="SemiBold" />
                                                    <TextBlock Text="{Binding Flag}" Foreground="{DynamicResource AccentBrush}" FontFamily="Consolas" Margin="7,0,0,0" />
                                                </StackPanel>
                                                <TextBlock Text="{Binding DisplayDescription}" Foreground="{DynamicResource MutedTextBrush}" FontSize="11.5" TextWrapping="Wrap" Margin="0,2,0,0" />
                                            </StackPanel>
                                            <TextBox Grid.Column="2" Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsSelected}" Visibility="{Binding HasValue, Converter={StaticResource BooleanToVisibilityConverter}}" Padding="7,5" VerticalAlignment="Center" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <StackPanel Grid.Row="2" Margin="18,12,18,16">
                            <TextBlock Text="ADVANCED OPTIONS" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource MutedTextBrush}" Margin="2,0,0,5" />
                            <TextBox Text="{Binding AdvancedOptions, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="{Binding IsBusy}" ToolTip="Self-contained Robocopy switches only, for example /FFT /COMPRESS" />
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="14" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="14" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Transfer progress" FontSize="16" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding StatusText}" Foreground="{DynamicResource MutedTextBrush}" Margin="0,3,0,0" />
                            </StackPanel>
                            <TextBlock Grid.Column="1" Text="{Binding OverallPercentText}" FontSize="22" FontWeight="SemiBold" Foreground="{DynamicResource AccentBrush}" VerticalAlignment="Center" />
                        </Grid>

                        <Grid Margin="0,18,0,5">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding OverallLabel}" FontWeight="SemiBold" />
                            <TextBlock Grid.Column="1" Text="{Binding TransferredText}" Foreground="{DynamicResource MutedTextBrush}" />
                        </Grid>
                        <ProgressBar Value="{Binding OverallPercent, Mode=OneWay}" IsIndeterminate="{Binding IsScanning}" />

                        <Grid Margin="0,15,0,5">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                            <TextBlock Text="Current file" FontWeight="SemiBold" />
                            <TextBlock Grid.Column="1" Text="{Binding CurrentFilePercentText}" Foreground="{DynamicResource MutedTextBrush}" />
                        </Grid>
                        <TextBlock Text="{Binding CurrentFile}" TextTrimming="CharacterEllipsis" ToolTip="{Binding CurrentFile}" Foreground="{DynamicResource MutedTextBrush}" Margin="0,0,0,6" />
                        <ProgressBar Value="{Binding CurrentFilePercent, Mode=OneWay}" />

                        <Grid Margin="0,18,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" /><ColumnDefinition Width="*" /><ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="SPEED" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource MutedTextBrush}" />
                                <TextBlock Text="{Binding SpeedText}" FontSize="14" FontWeight="SemiBold" Margin="0,3,0,0" />
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="TIME LEFT" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource MutedTextBrush}" />
                                <TextBlock Text="{Binding EtaText}" FontSize="14" FontWeight="SemiBold" Margin="0,3,0,0" />
                            </StackPanel>
                            <StackPanel Grid.Column="2">
                                <TextBlock Text="FILES" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource MutedTextBrush}" />
                                <TextBlock Text="{Binding FilesText}" FontSize="14" FontWeight="SemiBold" Margin="0,3,0,0" />
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>

                <Border Grid.Row="2" Style="{StaticResource CardStyle}">
                    <StackPanel>
                        <Grid>
                            <TextBlock Text="Command preview" FontSize="16" FontWeight="SemiBold" />
                            <Button Content="Copy" HorizontalAlignment="Right" Padding="11,5" Command="{Binding CopyCommandCommand}" />
                        </Grid>
                        <TextBox Text="{Binding CommandPreview, Mode=OneWay}" IsReadOnly="True" FontFamily="Cascadia Mono, Consolas" FontSize="11.5" TextWrapping="Wrap" MinHeight="58" MaxHeight="95" VerticalScrollBarVisibility="Auto" Margin="0,11,0,0" />
                        <TextBlock Text="{Binding ValidationText}" Foreground="{DynamicResource DangerBrush}" TextWrapping="Wrap" FontSize="11.5" Margin="2,8,2,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers><Trigger Property="Text" Value=""><Setter Property="Visibility" Value="Collapsed" /></Trigger></Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Border>

                <Border Grid.Row="4" Style="{StaticResource CardStyle}" Padding="0">
                    <Grid>
                        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                        <Grid Margin="18,15,18,10">
                            <StackPanel>
                                <TextBlock Text="Live Robocopy output" FontSize="16" FontWeight="SemiBold" />
                                <TextBlock Text="The complete transcript is also saved to your local app data." Foreground="{DynamicResource MutedTextBrush}" FontSize="11.5" Margin="0,2,0,0" />
                            </StackPanel>
                            <Button Content="Logs folder" HorizontalAlignment="Right" VerticalAlignment="Center" Padding="11,5" Command="{Binding OpenLogsFolderCommand}" />
                        </Grid>
                        <TextBox Grid.Row="1" Text="{Binding LogText, Mode=OneWay}" IsReadOnly="True" AcceptsReturn="True" TextWrapping="NoWrap" FontFamily="Cascadia Mono, Consolas" FontSize="11" BorderThickness="0,1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Padding="14,10" />
                        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="12,9">
                            <Button Content="Copy complete log" Padding="11,5" Margin="0,0,8,0" Command="{Binding CopyLogCommand}" />
                            <Button Content="Open log" Padding="11,5" Command="{Binding OpenLogCommand}" />
                        </StackPanel>
                    </Grid>
                </Border>
            </Grid>
        </Grid>

        <Border Grid.Row="2" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0">
            <Grid Margin="20,0">
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Tip: Review the command preview before using Mirror, Purge, or Move." FontWeight="SemiBold" />
                    <TextBlock Text="Stop permanently ends the current run; Pause keeps Robocopy in memory for Resume." Foreground="{DynamicResource MutedTextBrush}" Margin="0,3,0,0" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Content="▶  Start" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding StartCommand}" MinWidth="100" />
                    <Button Content="Ⅱ  Pause" Command="{Binding PauseCommand}" Margin="9,0,0,0" MinWidth="94" />
                    <Button Content="▶  Resume" Command="{Binding ResumeCommand}" Margin="9,0,0,0" MinWidth="100" />
                    <Button Content="■  Stop" Style="{StaticResource DangerButtonStyle}" Command="{Binding StopCommand}" Margin="9,0,0,0" MinWidth="92" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
