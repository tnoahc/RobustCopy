# UI mockups

- `GUI.html` is the interactive source mockup for the desktop interface.
- `UI MOCKUP.png` is the visual reference showing the intended rendered layout.

Generated comparison screenshots belong under `artifacts/` and are intentionally excluded from version control.
